<form method="post" action="{{ path('app_friend_group_delete', {'id': friend_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ friend_group.id) }}">
    <button class="btn delete-btn">Delete</button></form>

<style>
    .btn.delete-btn {
        background-color: #e74c3c;
        color: #ffffff;
        border: none;
        padding: 12px 25px;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s, transform 0.3s;
        box-shadow: 0px 2px 5px rgba(231, 76, 60, 0.2);
    }

    .btn.delete-btn:hover {
        background-color: #c0392b;
        transform: translateY(-2px);
    }

    .btn.delete-btn:active {
        transform: translateY(0px);
    }
</style>
